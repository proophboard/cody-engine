import * as React from 'react';
import {PlayInformationRuntimeInfo} from "@cody-play/state/types";
import {PageMode} from "@cody-play/app/pages/PlayStandardPage";
import {UiSchema} from "@rjsf/utils";

const PlayAlertView = (params: any, informationInfo: PlayInformationRuntimeInfo, pageMode: PageMode, hiddenView = false, uiSchemaOverride?: UiSchema, injectedInitialValues?: any) => {

};

export default PlayAlertView;

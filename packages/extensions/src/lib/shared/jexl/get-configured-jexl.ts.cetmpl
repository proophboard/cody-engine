import {Jexl} from "@event-engine/infrastructure/jexl/jexl";

export const extendJexlConfiguration = (jexl: Jexl): Jexl => {
  // [Jexl](https://github.com/TomFrost/Jexl) is used accross the app (fe, be) to execute user provided snippets in a specific context
  // It can be extended with custom functions and transforms (see README of github link above), which you can register here
  return jexl;
}
